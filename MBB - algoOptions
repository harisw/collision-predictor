#1 option
	use currRectMBR
	findMinMaxPoint
	getLargestPossibleAreafromMinMaxPoint()
	GetOverlappingNodeUsingR()

#2 option
	getMiniball from MBR
	findMinMaxPoint from supportPoints
	getLargestRfrom support Point()
	getOverlappingNodeUsingMiniballandR()


#Best option
	store maxRad on each node
	add the Radius value to each m_MBR value
	for queryAtT0, use the timeAtT(nextIntervalT, e.g: t5)

	queryAt:
		for(node in nodes)
			get overlapping pair of MBR: ()
	return all the entry in the smallest possible overlapping node

	#1 option:
		check collision of MBR's entry pairwise
	#2 option
		check collision of MBR's all entries